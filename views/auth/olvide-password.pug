extends ../layout/index

block contenido
        .py-10
            h1.text-4xl.my-10.font-extrabold.text-center E
                span.font-normal Commerce 
            h2.text-center.text-2xl.font-extrabold= pagina 

            .mt-8.mx-auto.max-w-md
                .bg-white.py-8.px-4.shadow
                    form.space-y-5(method="POST" action="/auth/olvide-password")
                        input(type="hidden" name="_csrf" value= csrfToken) 
                        div 
                            label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="email") Email de registro
                            input#email.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Tu Email" type="email" name="email") 

                        div.flex.items-center.justify-between
                            a.text-gray-400.text-xs(href="/auth/registro") ¿No tienes cuenta? Crea una
                            a.text-gray-400.text-xs(href="/auth/login") Inicia Sesión

                        input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-extrabold py-3 cursor-pointer"
                        type="submit" value="Enviar Instrucciones")  